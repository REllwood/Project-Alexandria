---
description: Draft an email / message / update grounded in the KB (correct names, dates, decisions — never invented)
argument-hint: "write a <email|update|message> to <who> about <topic> for <client>"
---

Follow the **kb-compose** skill (`skills/kb-compose/SKILL.md`): pick the client, pull the real facts from the KB, draft in the requested format/tone, never invent facts, add a "Sources (for your reference)" footer, and offer to save/export. Never send.

Request: $ARGUMENTS
